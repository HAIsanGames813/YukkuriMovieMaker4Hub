<Window x:Class="YukkuriMovieMaker4Hub.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YukkuriMovieMaker4Hub"
        Title="{x:Static local:Translate.InstallProgress}" Height="500" Width="600"
        Background="{DynamicResource ThemeBrush}"
        FontFamily="{DynamicResource AppFont}"
        BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <!-- テーマリソース（MainWindowと同じデフォルト値。ApplyThemeで上書きされる） -->
        <FontFamily x:Key="AppFont">Segoe UI</FontFamily>
        <SolidColorBrush x:Key="ThemeBrush"   Color="#252525"/>
        <SolidColorBrush x:Key="PanelBrush"   Color="#333333"/>
        <SolidColorBrush x:Key="ItemBrush"    Color="#444444"/>
        <SolidColorBrush x:Key="AccentBrush"  Color="#2E7D32"/>
        <SolidColorBrush x:Key="TextBrush"    Color="White"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="BorderBrush"  Color="#555555"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{DynamicResource ItemBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" CornerRadius="3">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{DynamicResource TextBrush}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Text="{x:Static local:Translate.Preparing}"
                   FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <ProgressBar Grid.Row="1" x:Name="DownloadProgressBar"
                     Height="20" Minimum="0" Maximum="100"
                     Background="{DynamicResource PanelBrush}"
                     Foreground="{DynamicResource AccentBrush}"
                     BorderThickness="0"/>

        <TextBlock Grid.Row="2" x:Name="CountText" Text="0 / 0"
                   Foreground="{DynamicResource SubTextBrush}"
                   FontSize="11" HorizontalAlignment="Right" Margin="0,5,0,10"/>

        <GroupBox Grid.Row="3" Header="Readme"
                  Foreground="{DynamicResource SubTextBrush}"
                  BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <TextBox x:Name="ReadmeBox"
                         Background="{DynamicResource PanelBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderThickness="0" IsReadOnly="True"
                         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True" Padding="10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Top" Margin="0,5,5,0">
                    <Button x:Name="PrevBtn" Content="◀" Style="{StaticResource NavButton}"
                            Click="PrevReadme_Click" Margin="0,0,5,0"/>
                    <TextBlock x:Name="ReadmePageText" Text="0 / 0"
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center" Margin="5,0"/>
                    <Button x:Name="NextBtn" Content="▶" Style="{StaticResource NavButton}"
                            Click="NextReadme_Click" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <Button Grid.Row="4" x:Name="FinalCloseButton"
                Content="{x:Static local:Translate.CompleteAndClose}"
                Click="FinalCloseButton_Click"
                Width="140" Height="35" HorizontalAlignment="Right"
                Margin="0,15,0,0" Visibility="Collapsed"
                FontWeight="Bold" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{DynamicResource AccentBrush}"
                            CornerRadius="4" Padding="10,5">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="White" HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
